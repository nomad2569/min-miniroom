# 인터랙티브 자기소개 사이트 — One-Shot Prompt

사용법: ① INPUT을 채운다(비운 항목은 검증된 기본값 사용) → ② 문서 전체를 Claude Code에
붙여넣는다. 사전 준비물은 `required` 두 가지뿐.

이 프롬프트는 두 층으로 되어 있다.
- **원리** — 실전에서 밟은 함정들의 회피법. 어떤 스타일을 골라도 지켜라.
- **기본값** — 한 번 완성해본 검증된 조합. INPUT으로 얼마든지 갈아끼워라.

---

## INPUT

```yaml
required:
  name: ""                  # 이름/닉네임
  team: ""                  # 소속
  language: ""              # 발표 언어 (보조 자막 언어는 sub_language로 변경 가능)
  openai_api_key: ""        # .env에 OPENAI_API_KEY=... 로 저장해뒀다고 쓰기
  items:                    # 스토리 8~15개. 오브젝트 1개 = 발표 1스텝. 주제는 잘게 분리.
    - { topic: "인사",   story: "", prop: "" }   # story: 1~3문장, 아무 언어나(번역됨)
    - { topic: "",       story: "", prop: "" }   # prop 비우면 어울리는 소품 제안받음
  ending_message: ""        # 마지막 인사 + 포부

style:                      # ── 전부 선택. 비우면 [기본값] ──
  concept: ""               # 무대 콘셉트 [아이소메트릭 미니룸] — 우주선, 편의점, 책상 위
                            # 디오라마, 지하철 칸… 뭐든. "발표 스텝 = 무대 위 오브젝트"만 유지
  art_style: ""             # [16비트 JRPG 픽셀아트] — 손그림 잉크, 로우폴리 3D, 클레이…
  mood: ""                  # [다크 나이트룸 + 단일 액센트 #E8474F] — 파스텔, 네온, 종이질감…
  sound: ""                 # [자작 8비트 칩튠 합성] — lo-fi, 무음…
  retro_deco: ""            # [방문자 카운터·재화 수집·컴플리트 배너] — 콘셉트에 맞게 변형/제거
  gag_line: ""              # 오프닝에 크게 박을 문구 — 비우면 대상 문화권 밈 제안받음
  image_model: ""           # [gpt-image-2] — 다른 모델이면 후처리 원리는 동일 적용

optional:
  sub_language: "English"   # 소자막 언어
  photo_me: ""              # 본인 사진 경로(assets/reference/) — 캐릭터 닮음용
  appearance: ""            # 사진 없을 때 외모 묘사
  extra_characters: []      # 동반 등장인물 (예: {who: 배우자, photo: 경로, scene: 게임 듀오})
  logo_url: ""              # 엔딩 로고
  deploy: "github-pages"    # github-pages | none
```

---

## MISSION

INPUT의 콘셉트 무대 위에 오브젝트들을 배치하고, 그 위에 슬라이드처럼 진행되는 발표 덱을
얹은 자기소개 사이트를 완성하라. 완성 기준: 보는 사람이 디테일에 감탄하는 수준.

## 원리 1 — 오프라인 생존 (발표장에 와이파이는 없다)

- 개발용 파일과 별개로, 의존성 전부를 인라인한 **오프라인 단일 HTML**을 빌드 스크립트로
  산출하라. `file://`에서 ES module import는 CORS로 막힌다 — 인라인만이 답이다.
- 외부 리소스(CDN·폰트·오디오 파일) 금지. 사운드가 필요하면 Web Audio로 합성하라.
- 모든 생성 에셋에 프로시저럴 폴백 — 에셋이 없어도 사이트는 항상 동작해야 한다.

## 원리 2 — 이미지 생성 함정

- 투명 배경 파라미터를 믿지 마라: 프롬프트로 단색 마젠타(#FF00FF) 배경을 강제하고
  후처리 크로마키(r>g+80 && b>g+40 → 알파 이진화 → 연결 컴포넌트는 최대치의 2% 이상
  전부 보존 → bbox 크롭, 원본 보존)로 뽑아라.
- 모든 프롬프트에 "occupies only ~70% of the canvas with wide margins" — 없으면 잘린다.
- 닮음: images/edits에 [기존 캐릭터 에셋(화풍 고정) + 사진(닮음)] 2장을 함께 투입.
- 생성은 병렬로. 결과물은 색 양자화로 압축. 스타일 일관성은 공통 프리픽스 프롬프트로.

## 원리 3 — 씬 (2D 에셋을 3D 무대에 세울 때)

- 서 있는 에셋을 빌보드로 만들지 마라 — 카메라 따라 눕다가 배경을 뚫는다.
  고정 수직 플레인 + 바닥 그림자. 회전된 플레인은 폭/2×sin(회전각)만큼 배경에서 이격.
- 벽걸이류는 벽 평행 고정 플레인.

## 원리 4 — 발표 UX

- 진행: 인트로 → 스텝들 → 엔딩. 진행 표시(레일), 키보드(Space/←→/숫자)와 클릭 동기화,
  빈 공간 클릭 = 카드 닫기. 마우스만으로도, 키보드만으로도 완주 가능해야 한다.
- 스텝마다 시선 유도: 카메라 줌+팬, 대상 마커, 캐릭터가 대상을 향해 반응(+스텝 전용 포즈).
- **철칙: 설명 카드는 강조 오브젝트와 캐릭터를 절대 가리지 않는다.** 둘의 화면 rect를
  투영해 후보 위치를 겹침 면적으로 스코어링해 배치하라. 세로 화면은 씬을 팬해 공간 확보.
- 오프닝 텍스트는 피팅 완료 전 opacity 0 — 큰 파일 파싱 중 미피팅 글자가 번쩍인다.
- 대상 문화권 뉘앙스 검수 1회: 약어는 풀어 쓰고(예: LoL→리그 오브 레전드 병기),
  밈은 그 세대가 아는 것으로.

## 원리 5 — 성능 (장시간 켜두는 발표용 — 발열 금지)

- **수요 기반 렌더링**: 활동(입력·카메라·애니메이션·파티클) 시에만 풀 프레임, 유휴 시 절반
  이하로 강하(wake 타이머 패턴). 프레임 카운터를 노출해 실측으로 검증하라.
- antialias·그림자·DPR은 스타일이 허용하는 최소로. low-power 힌트.

## 원리 6 — 프라이버시·검증

- API 키와 인물 사진은 .gitignore + 커밋 전 스테이징 grep. 사진 원본은 절대 공개 금지.
- Playwright로 스플래시→전 스텝→엔딩 워크(데스크톱+모바일 뷰포트), 콘솔 에러 0,
  잘림·겹침은 스크린샷으로 직접 눈으로 확인. 오프라인 파일도 동일 검증.

## 기본값 레시피 (style을 비웠을 때 — 검증된 조합)

- 무대: 다크 나이트룸 미니룸(밤하늘 창, 가랜드 조명, 먼지 입자), 아이소 Ortho + 드래그 ±24°.
- 연출: RPG 타이프라이터 카드 + 소자막, 스텝별 캐릭터 연기 포즈, 리액션 이모트,
  엔딩 절+콘페티+팡파레+로고, "PRESS START" 스플래시 + 카운터 롤업.
- 사운드: blip·코인·팡파레·8비트 BGM 루프(기본 꺼짐, 토글 버튼이 스스로를 광고).
- 성능 수치: 활동 33fps / 유휴 15fps, DPR ≤1.25.

## 진행 순서와 완료 조건

1. 스캐폴드+씬(폴백 오브젝트) → 2. 발표 시스템 → 3. 에셋 생성·교체 → 4. 오프닝·사운드 →
5. 검증 → 6. 배포(선택, public 고지).

- [ ] 오프라인 단일 HTML이 와이파이 없이 전 기능 동작
- [ ] 모든 아이템: 전용 오브젝트 + 전용 반응 + 발표 언어 스토리 + 소자막
- [ ] 카드가 주인공(오브젝트·캐릭터)을 가리는 화면 0건 (스크린샷 증빙)
- [ ] 콘솔 에러 0, 유휴 프레임 강하 실측, 모바일 정상
- [ ] 키·사진 git 부재 (grep 증빙)

스크린샷으로 자가 검수하며 끝까지 진행하고, 취향 갈림길에서만 짧게 물어라.
