# 픽셀 미니룸 자기소개 사이트 — One-Shot Prompt

사용법: ① 아래 INPUT 블록을 채운다 → ② 이 문서 전체를 Claude Code에 붙여넣는다 → ③ 끝.
사전 준비물은 INPUT의 `required` 두 가지뿐.

---

## INPUT — 이것만 채우면 됨

```yaml
required:
  name: ""                  # 이름/닉네임 (예: Min)
  team: ""                  # 소속 (예: TimeTree Korea · 10x)
  language: ""              # 발표 언어 (예: 일본어) — 영어 소자막은 자동 병기
  openai_api_key: ""        # 프로젝트 .env에 OPENAI_API_KEY=... 로 저장해뒀다고 쓰기
  items:                    # 발표 스토리 8~15개. 소품 1개 = 발표 1스텝.
    # topic: 주제 / story: 할 말 1~3문장(한국어로 써도 됨, 발표 언어로 번역됨)
    # prop: 방에 놓을 소품 — 비워두면 에이전트가 어울리는 소품을 제안
    - { topic: "인사",        story: "",  prop: "캐릭터 본인" }
    - { topic: "출생지",      story: "",  prop: "" }
    - { topic: "닉네임 유래", story: "",  prop: "" }
    - { topic: "커리어 1",    story: "",  prop: "" }
    - { topic: "취미 1",      story: "",  prop: "" }
    # …자유롭게 추가. 주제는 최대한 잘게 쪼갤 것(한 소품 = 한 이야기)
  ending_message: ""        # 마지막 인사 + 포부 한 줄

optional:
  photo_me: ""              # 본인 사진 경로 (assets/reference/me.png) — 캐릭터 닮음용
  appearance: ""            # 사진 없을 때 외모 묘사 (머리 모양·색, 옷차림)
  extra_characters: []      # 함께 등장할 인물 (예: {who: 배우자, photo: 경로, scene: 게임 듀오})
  logo_url: ""              # 엔딩 카드에 넣을 회사/제품 로고 이미지 URL
  accent_color: "#E8474F"   # 다크 룸에 쓸 단일 액센트 컬러
  gag_line: ""              # 상단에 박을 개그 문구 (예: 유명 밈) — 없으면 제안받기
  deploy: "github-pages"    # github-pages | none
```

---

## MISSION

위 INPUT으로 "픽셀 미니룸 자기소개 프레젠테이션 사이트"를 완성하라.
아이소메트릭 3D 방(three.js) 위에 슬라이드처럼 진행되는 발표 덱. 스타일은 16비트 JRPG
픽셀아트 + 다크 나이트룸. 완성 기준: 보는 사람이 디테일에 감탄하는 수준.

## 아키텍처 (고정)

- 개발용 `index.html`+`app.js`+로컬 `three.module.js`(CDN 금지) / `scripts/build.py`가
  전부 인라인한 **오프라인 단일 HTML**을 산출. 이유: `file://`에서 ES module import는
  CORS로 막힌다. 발표장엔 와이파이가 없다고 가정하라.
- AI 에셋: 매니페스트 기반 생성 스크립트 → PNG → base64로 `gen-assets.js` 베이크.
  **모든 에셋에 프로시저럴 3D 폴백** — 에셋이 없어도 사이트는 항상 동작해야 한다.
- `.env`와 `assets/reference/`(사진)는 .gitignore. 커밋 전 스테이징을 grep으로 검사.

## 이미지 생성 규칙 (gpt-image-2, quality high)

- 투명 배경 파라미터는 불안정 → 프롬프트로 마젠타 배경(#FF00FF, no shadow/glow/
  vignette/floor)을 강제하고 후처리 크로마키: r>g+80 && b>g+40 → 알파 이진화 →
  연결 컴포넌트는 최대치의 2% 이상 전부 보존 → bbox 크롭. 원본은 .orig.png 보존.
- 모든 프롬프트에 "occupies only 70% of the canvas with wide margins" — 없으면 잘린다.
- 닮음이 필요하면 `/v1/images/edits`에 [기존 스프라이트(화풍 고정) + 사진(닮음)] 2장 투입.
- 병렬 생성(`&`+wait). 목록: 캐릭터 기본·절(오지기)·**아이템별 연기 포즈**(경례·스윙 등),
  가구 전부, 리액션 이모트(!,♥,★,♪…). 128색 양자화로 압축.

## 씬 규칙

- Ortho 아이소 뷰, 드래그 ±24°. 다크 벽 2면+마루, 밤하늘 창(달·별), 가랜드 조명, 먼지 입자,
  accent_color 하나만 포인트로.
- 바닥 가구는 빌보드 금지(카메라 따라 눕다가 벽 뚫음) → 고정 수직 플레인 + 타원 블롭 그림자.
  45° 플레인은 폭/2×0.707만큼 벽에서 이격. 벽걸이는 벽 평행 플레인.

## 프레젠 시스템

- 인트로 카드 → 아이템 스텝들 → 엔딩 카드. 하단 진행 레일(dot+라벨+‹›),
  Space/←→/숫자키, 가구 직접 클릭 동기화, 빈 바닥 클릭 = 카드 닫기.
- 스텝마다: 카메라 줌+팬, 대상 위 바운스 마커, 발밑 펄스 링, 가구 팝 바운스,
  캐릭터 플립+점프+**해당 포즈로 스왑**, 머리 위 이모트.
- 카드: RPG 타이프라이터(2자/13ms) + 영어 소자막 + 타이틀 폰트 자동 피팅(이진탐색).
- **철칙: 카드는 강조 아이템과 캐릭터를 절대 가리지 않는다** — 둘의 화면 rect를 매 프레임
  투영, 후보 위치(아이템 옆→코너 / 모바일 상·하존)를 겹침 면적으로 스코어링해 선택.
  모바일은 씬 전체를 위로 팬해 공간을 만든다.
- 엔딩: 절 사이클 + 옆에서 떠오르는 하트(머리 위 금지) + 콘페티 + 팡파레 + 로고.

## 오프닝·사운드

- 스플래시: 타이틀을 뷰포트 폭에 피팅 + gag_line 크게 + "▶ PRESS START".
  피팅 전 opacity 0(미피팅 텍스트 번쩍임 방지). 해제 시 방문자 카운터 슬롯머신 롤업.
- 사운드 전부 Web Audio 합성(파일 0): blip·코인·타이프 틱·팡파레·자작 8비트 BGM 루프
  (기본 꺼짐, BGM 바가 스스로 버튼임을 광고). 레트로 장식: 방문자 카운터, 재화 수집,
  컴플리트 배너.

## 성능·모바일

- antialias off, DPR ≤1.25, shadowMap off, low-power. **수요 기반 렌더링**: 활동 시 33fps,
  유휴 15fps(wake 타이머). 프레임 카운터를 DOM에 노출해 실측 검증.
- 세로: 방 가장자리 크롭(축소 금지), 카드 상·하존 자동, 타이포 축소, 터치 지원.

## 진행 순서

1. 스캐폴드 + 씬 + 프로시저럴 폴백 가구 → 스크린샷 검수
2. 프레젠 시스템(레일·카메라·카드·철칙) → 전 스텝 키보드 워크 검증
3. 에셋 병렬 생성 → 키잉 → 베이크 → 교체 → 배치 겹침·잘림 스크린샷 검수
4. 오프닝·사운드·엔딩 폴리시
5. 검증: Playwright로 스플래시→전 스텝→엔딩 (1440 + 390), 콘솔 에러 0
6. deploy가 github-pages면: public repo 고지 후 배포 + OG 메타/이미지, 라이브 확인

## 완료 조건 (전부 만족해야 끝)

- [ ] 오프라인 단일 HTML이 와이파이 없이 열리고 전 기능 동작
- [ ] 모든 아이템: 전용 소품 + 전용 캐릭터 포즈 + 발표 언어 스토리 + 영어 소자막
- [ ] 카드가 강조 아이템/캐릭터를 가리는 화면이 단 하나도 없음 (스크린샷 증빙)
- [ ] 콘솔 에러 0, 유휴 15fps 실측, 모바일 390px 정상
- [ ] .env·사진이 git에 없음 (grep 증빙)

작업 내내 스크린샷으로 자가 검수하고, 취향 갈림길(아트 방향·문구 톤)에서만 짧게 물어라.
