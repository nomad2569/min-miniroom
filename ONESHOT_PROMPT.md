# 픽셀 미니룸 자기소개 사이트 — One-Shot Prompt

> Claude Code에 아래 프롬프트를 통째로 붙여넣고, `{{ }}` 부분만 본인 걸로 채우세요.
> 필요 준비물: OpenAI API 키(이미지 생성용), 본인 캐릭터 레퍼런스 사진 1장(선택).

---

나만의 "픽셀 미니룸 자기소개 프레젠테이션 사이트"를 만들어라. 싸이월드 미니룸 콘셉트의
아이소메트릭 3D 방(three.js) 위에 슬라이드처럼 진행되는 발표 덱을 얹은 단일 웹사이트다.
완성 기준은 "보는 사람이 '이 사람 이거에 미쳐서 만들었구나'라고 느끼는 수준"이다.

## 0. 내 정보 (이걸로 콘텐츠를 채워라)

- 이름/닉네임: {{이름}} / 소속: {{회사·팀}}
- 발표 대상·언어: {{예: 일본 본사 동료, 일본어(영어 소자막 병기)}}
- 스토리 아이템 목록 (각각 방 안의 소품 하나 = 발표 한 스텝):
  {{예시 — 출생지 액자 / 닉네임 유래 명패 / 게임기(입덕 계기) / 학사모(전공·진로) /
  군모(병역) / 커리어 소품들 / 취미 소품들 / 마무리 인사. 8~15개. 주제는 최대한 잘게 분리}}
- 캐릭터 레퍼런스 사진: {{있으면 assets/reference/me.png 에 넣었다고 알려주기}}
- 엔딩 멘트: {{예: 앞으로 잘 부탁드립니다 + 포부 한 줄}} + 회사 로고 URL: {{선택}}

## 1. 아키텍처 (그대로 따를 것)

- `index.html` + `app.js` + 로컬 `three.module.js`(CDN 금지, curl로 받아둠) — 개발용.
- `scripts/build.py`: 셋을 전부 인라인한 **오프라인 단일 HTML**을 뽑는다.
  이유: `file://`에서 ES module import는 CORS로 죽는다. 발표장은 와이파이가 없다고 가정.
- AI 에셋은 `scripts/generate_assets.py`(매니페스트 방식) → PNG →
  `scripts/bake_assets.py`가 base64로 `gen-assets.js`에 굽는다.
  **모든 에셋에 프로시저럴 3D 폴백**을 두어 에셋이 없어도 사이트가 항상 돈다.
- `.env`에 OPENAI_API_KEY. `.env`와 `assets/reference/`(사진)는 반드시 .gitignore.

## 2. 이미지 생성 규칙 (전부 실전에서 얻은 함정 회피법)

- 모델: `gpt-image-2`, quality high. 스타일: 16비트 SNES JRPG 픽셀아트, 다크 나이트룸 팔레트.
- **투명 배경 파라미터를 믿지 마라.** 프롬프트로 "flat solid magenta #FF00FF background,
  no shadow/glow/vignette/floor"를 강제하고, 후처리 스크립트로 마젠타 크로마키
  (r>g+80 && b>g+40) → 알파 이진화 → **최대 컴포넌트의 2% 이상은 전부 보존**(느낌표의
  점이 날아가지 않게) → 콘텐츠 bbox 크롭. 원본은 `.orig.png`로 보존.
- **"the object occupies only 70% of the canvas with wide margins"를 항상 넣어라.**
  안 넣으면 팔다리·모니터가 캔버스 밖으로 잘린 채 생성된다.
- 캐릭터 닮음이 필요하면 `/v1/images/edits`에 레퍼런스 2장(기존 스프라이트=화풍 고정 +
  사진=닮음)을 함께 넣어라. 사진은 절대 커밋 금지.
- 생성은 **병렬로**(shell `&` + wait). 직렬은 장당 40~70초라 지옥이다.
- 생성 목록: 캐릭터 기본/절(오지기)/섹션별 포즈(아이템당 1개, 경례·스윙 등 연기 포즈),
  가구 전부, 리액션 이모트(!,♥,★,♪ 등), 128색 양자화로 용량 압축.

## 3. 씬 구성 (렌더링 함정)

- OrthographicCamera 아이소 뷰, 드래그 ±24° 회전. 방: 다크 벽 2면+마루, 밤하늘 창문(달·별),
  가랜드 조명 반짝임, 러그, 먼지 입자. 단일 액센트 컬러 1개(예: 레드)로 시크하게.
- **바닥 가구는 빌보드 Sprite 금지** — 카메라 따라 뒤로 누우면서 벽을 뚫는다.
  기본 카메라 방향으로 고정한 수직 플레인 + 발밑 타원 블롭 그림자.
- 45° 돌린 플레인은 폭의 절반×0.707만큼 벽을 침범한다 — 벽에서 그만큼 띄워 배치.
- 벽걸이(액자·달력 등)는 벽 평행 고정 플레인.

## 4. 프레젠 시스템

- 인트로 카드 → N개 스텝 → 엔딩 카드. 하단 진행 레일(dot+라벨+‹›), Space/→/←/숫자키,
  가구 직접 클릭도 레일과 동기화, 빈 바닥 클릭 = 카드 닫기.
- 스텝마다: 카메라 줌+팬, 대상 위 바운스 마커, 발밑 펄스 링, 가구 팝 바운스,
  캐릭터가 대상 향해 플립+점프 후 **섹션 포즈로 스왑**, 머리 위 픽셀 이모트.
- 스토리 카드: RPG 타이프라이터(2자/13ms, 깜빡이는 커서), 주 언어 본문 + 작은 영어 병기,
  리스트 항목은 스태거 페이드인. 타이틀은 이진탐색 폰트 피팅.
- **철칙: 카드는 어떤 상황에서도 '현재 강조 아이템'과 '캐릭터'를 가리면 안 된다.**
  둘의 화면 사각형을 매 프레임 투영해 후보 위치(아이템 옆→코너/모바일 상·하존)를
  겹침 면적으로 스코어링, 겹침 0을 선택. 모바일은 씬 전체를 위로 팬해서 공간을 만든다.
- 엔딩: 절(오지기) 스프라이트 2.4초 사이클 + 옆에서 떠오르는 하트(머리 위 금지) +
  콘페티 + 팡파레 + 회사 로고.

## 5. 오프닝 & 사운드 (완성도의 핵심)

- 스플래시: 거대 타이틀을 뷰포트 폭에 이진탐색 피팅 + 깜빡이는 "▶ PRESS START".
  **피팅 전에는 opacity 0** — 큰 파일 파싱 중 작은 글자가 먼저 번쩍하는 공포체험 방지.
  해제 시 방문자 카운터가 슬롯머신처럼 롤업.
- 사운드는 **전부 Web Audio 합성**(파일 0개 = 오프라인 무결): 스텝 blip, 코인음,
  타이프 틱, 팡파레, 자작 8비트 BGM 루프(기본 꺼짐, BGM 바 클릭 토글 + 상태 문구).
- 레트로 장식: 방문자 카운터(검은 바탕 녹색 디지트), 도토리(또는 유사 재화) 수집,
  전부 모으면 깜빡이는 축하 배너.

## 6. 성능 (노트북 발열 방지 — 두 번 지적받은 부분)

- antialias off, DPR ≤1.25, 픽셀 씬에서는 shadowMap off, powerPreference 'low-power'.
- **수요 기반 렌더링**: 입력/카메라 이동/캐릭터 동작/파티클 있을 때만 33fps, 유휴 15fps.
  wake() 타이머 패턴. 프레임 카운터를 DOM에 노출해 실측으로 검증하라.

## 7. 모바일

- 세로: 방 가장자리 크롭(축소 금지), 씬 위로 팬, 카드는 상·하존 자동 선택 + 본문 스크롤,
  타이포 한 단계 축소, 터치 탭/드래그 지원.

## 8. 검증·배포 (끝났다고 말하기 전에 전부 통과시켜라)

- Playwright: 스플래시→전 스텝 워크→엔딩, 콘솔 에러 0, 데스크톱 1440 + 모바일 390 스샷,
  잘림·겹침을 스크린샷으로 직접 눈으로 확인하고 고칠 것.
- 오프라인 파일을 http로 띄워 동일 검증(인라인이라 file://과 등가).
- GitHub Pages 배포(public repo 주의 고지), OG 메타+이미지, 커밋 전 `.env`/사진
  스테이징 여부 반드시 grep.
- 마지막 안내: 발표는 로컬 오프라인 파일로, 와이파이 끄고 리허설.

작업 중 스크린샷을 자주 찍어 스스로 검수하고, 내 확인이 필요한 취향 갈림길
(아트 방향, 문구 톤)에서만 짧게 물어라. 나머지는 끝까지 알아서 진행하라.
