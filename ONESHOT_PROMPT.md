# 픽셀 미니룸 자기소개 사이트 — One-Shot Prompt

> Claude Code에 아래를 붙여넣고 `{{ }}`만 채우세요.
> 준비물: OpenAI API 키(.env), 캐릭터 레퍼런스 사진 1장(선택).

---

미니룸 콘셉트의 아이소메트릭 3D 방(three.js) 위에 슬라이드처럼 진행되는 발표 덱을 얹은
"픽셀 자기소개 사이트"를 만들어라. 완성 기준: 보는 사람이 디테일에 감탄하는 수준.

**내 정보**
- 이름/소속: {{이름 · 회사/팀}}
- 발표 대상·언어: {{예: 일본 동료, 일본어 + 영어 소자막}}
- 스토리 아이템(소품 1개 = 발표 1스텝, 8~15개, 주제는 잘게 분리):
  {{예: 출생지 액자, 닉네임 명패, 게임기, 학사모, 군모, 커리어 소품, 취미 소품…}}
- 캐릭터 사진: {{assets/reference/me.png 유무}} / 엔딩 멘트·로고: {{선택}}

**아키텍처**
- 개발용 `index.html`+`app.js`+로컬 `three.module.js` / `build.py`로 전부 인라인한
  **오프라인 단일 HTML** 산출(`file://`은 module import가 CORS로 막힘. 발표장 와이파이 없다고 가정).
- AI 에셋: 매니페스트 생성 스크립트 → PNG → base64로 `gen-assets.js`에 베이크.
  모든 에셋에 프로시저럴 3D 폴백 → 에셋 없어도 항상 동작.
- `.env`와 사진 폴더는 .gitignore. 커밋 전 스테이징 grep으로 확인.

**이미지 생성 (gpt-image-2, quality high, 16비트 JRPG 픽셀아트)**
- 투명 배경 파라미터는 불안정 → 마젠타 배경(#FF00FF)을 프롬프트로 강제 후 크로마키
  (r>g+80 && b>g+40, 알파 이진화, 컴포넌트는 최대치의 2% 이상 전부 보존, bbox 크롭).
- 프롬프트에 항상: "occupies only 70% of the canvas with wide margins" (안 넣으면 잘림),
  "no shadow/glow/vignette/floor".
- 닮음: `/v1/images/edits`에 기존 스프라이트(화풍)+사진(닮음) 2장 투입.
- 생성은 병렬(`&`+wait). 목록: 캐릭터 기본·절·섹션별 연기 포즈, 가구 전부, 이모트.
  128색 양자화로 압축.

**씬**
- Ortho 아이소 뷰, 드래그 ±24°. 다크 나이트룸(밤하늘 창, 가랜드 조명, 먼지 입자),
  액센트 컬러 1개.
- 바닥 가구는 빌보드 금지(뒤로 누워 벽 뚫음) → 고정 수직 플레인 + 타원 블롭 그림자.
  45° 플레인은 폭/2×0.707만큼 벽 이격. 벽걸이는 벽 평행 플레인.

**프레젠**
- 인트로 → N스텝 → 엔딩. 하단 레일(dot+라벨+‹›), Space/←→/숫자키, 가구 클릭 동기화,
  빈 바닥 클릭 = 카드 닫기.
- 스텝마다: 카메라 줌+팬, 바운스 마커, 펄스 링, 가구 팝, 캐릭터 플립+점프+포즈 스왑, 이모트.
- 카드: RPG 타이프라이터(2자/13ms) + 영어 소자막 + 타이틀 폰트 자동 피팅.
- **철칙: 카드는 강조 아이템과 캐릭터를 절대 가리지 않는다** — 둘의 화면 rect를 투영해
  후보 위치를 겹침 면적으로 스코어링. 모바일은 씬을 위로 팬해 공간 확보.
- 엔딩: 절 스프라이트 사이클 + 옆에서 뜨는 하트 + 콘페티 + 팡파레 + 로고.

**오프닝·사운드**
- 스플래시: 타이틀을 뷰포트 폭에 이진탐색 피팅 + "▶ PRESS START". 피팅 전 opacity 0
  (미피팅 텍스트 번쩍임 방지). 해제 시 방문자 카운터 슬롯머신 롤업.
- 사운드 전부 Web Audio 합성(파일 0): blip·코인·타이프 틱·팡파레·8비트 BGM 루프
  (기본 꺼짐, BGM 바 토글). 레트로 장식: 카운터, 재화 수집, 컴플리트 배너.

**성능·모바일**
- antialias off, DPR ≤1.25, shadowMap off, low-power. 수요 기반 렌더: 활동 시 33fps,
  유휴 15fps(wake 타이머). 프레임 카운터 노출해 실측.
- 세로 화면: 방 크롭(축소 금지), 카드 상·하존 자동, 타이포 축소, 터치 지원.

**검증·배포**
- Playwright로 전 스텝 워크(1440 + 390), 콘솔 에러 0, 스크린샷으로 잘림·겹침 직접 확인.
- 오프라인 파일 동일 검증 → GitHub Pages 배포(public 고지) + OG 메타/이미지.
- 발표는 로컬 오프라인 파일, 와이파이 끄고 리허설 안내.

스크린샷으로 자가 검수하며 끝까지 진행하고, 취향 갈림길(아트 방향·문구 톤)에서만 짧게 물어라.
