# 이미지 생성 API 키 — 하나만 채우면 됨.
# 둘 다 있으면 GEMINI 우선. IMAGE_PROVIDER 로 강제 지정 가능.
#
# 사용법:
#   cp .env.example .env  →  키 입력  →  python3 scripts/generate_assets.py

GEMINI_API_KEY=
OPENAI_API_KEY=

# 선택: gemini | openai
# IMAGE_PROVIDER=gemini
